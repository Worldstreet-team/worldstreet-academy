/**
 * The program page's one section-title size (h2) — well below the hero's
 * display title, above card titles, and the size the package ladder already
 * used, so the ladder reads the same on the signed-in course page. Every
 * section on the program page speaks at this volume except the shared FAQ,
 * which keeps the landing's.
 */
export const PROGRAM_H2 = "font-display text-2xl font-semibold leading-tight tracking-[-0.015em] text-ws-primary"

/**
 * Clearance for a section jumped to by the section nav: the stuck site navbar
 * (56px, 64px from `sm`) + the section nav (48px) + breathing room.
 */
export const SECTION_SCROLL_MT = "scroll-mt-[7.5rem] sm:scroll-mt-[8.5rem]"
